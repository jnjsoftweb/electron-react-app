```sh
cd C:\JnJ-soft\Playground\electron\electron-react-app

# create
github -e pushRepo -n electron-react-app -u jnjsoftweb -d "electron app in reactjs(typescript)"

# copy
github -e pushRepo -n electron-react-app -u jnjsoftweb
npm install
```